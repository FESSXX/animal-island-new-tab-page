# Browser Extension

Build:

```bash
npm run build
```

Load `.output/chrome-mv3/` as an unpacked extension in Chrome or Edge. No deployment is required.

For a distributable zip:

```bash
npm run zip
```
